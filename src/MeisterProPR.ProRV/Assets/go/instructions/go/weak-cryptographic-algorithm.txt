Review focus: Use of a broken or weak cryptographic algorithm

What to look for:
- Using broken or weak cryptographic algorithms can compromise security.
- Using weak cryptographic algorithms can leave data vulnerable to being decrypted or forged by an attacker. Many cryptographic algorithms provided by cryptography libraries are known to be weak. Using such an algorithm means that encrypted or hashed data is less secure than it appears to be.
- Keep the check only when the changed code appears to introduce, remove, bypass, or misuse the risky behavior itself.
- Prefer explicit code evidence such as API calls, removed guards, changed data flow steps, changed arguments, or changed signatures over broad similarity.

Relevance threshold:
- Drop this check if the diff only changes naming, comments, formatting, or unrelated plumbing.
- Do not keep this check solely because the file belongs to a broad category like security, configuration, or API usage.
- This is direct-pattern focused. Keep it only when the diff shows the unsafe call, broken contract, or misuse pattern itself.

Reference examples:
- Reference example (`Crypto.go`):
```
package main

import (
	"crypto/aes"
	"crypto/des"
)

func EncryptMessageWeak(key []byte, message []byte) (dst []byte) {
	// BAD, DES is a weak crypto algorithm
	block, _ := des.NewCipher(key)
	block.Encrypt(dst, message)
	return
}

func EncryptMessageStrong(key []byte, message []byte) (dst []byte) {
	// GOOD, AES is a strong crypto algorithm
	block, _ := aes.NewCipher(key)
	block.Encrypt(dst, message)
	return
}
```
